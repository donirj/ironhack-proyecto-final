// const isLoggedIn = (req, res, next) => {
//   //SI NO ESTAS LOGGEADO Y QUIERES ACCEDER A UNA AREA
//   if (!req.session.currentUser) {
//     return res.redirect("/auth/login");
//   }

//   next();
// };

// module.exports = isLoggedIn;
